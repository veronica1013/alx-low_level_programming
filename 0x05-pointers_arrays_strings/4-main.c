#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;

    str = "This is s function that prints string in reverse";
    print_rev(str);
    return (0);
}
